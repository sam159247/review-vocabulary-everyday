function pushEnglishVocabulary() {
  const sheetId = '';
  const webhooksUrl = '';
  main(sheetId, webhooksUrl);
}

function pushJapaneseVocabulary() {
  const sheetId = '';
  const webhooksUrl = '';
  main(sheetId, webhooksUrl);
}
